interface IAboutUsPicture {
    id: string;
    image: any;
    gridName: string;
}

interface IAboutUsText {
    id: string;
    text: string;
    gridName: string;
}

export type { IAboutUsPicture, IAboutUsText };
