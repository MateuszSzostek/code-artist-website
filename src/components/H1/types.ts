import { FontSizeType, ColorType } from "./../../GlobalTypes";

interface IH1 {
    headerStyles: {
        size: FontSizeType;
        color: ColorType;
        verticalMargin?: string;
    };
}

export type { IH1 };
