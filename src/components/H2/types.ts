import { FontSizeType, ColorType } from "../../GlobalTypes";

interface IH2 {
    headerStyles: {
        size: FontSizeType;
        color: ColorType;
        center?: boolean;
        underline?: boolean;
    };
}

export type { IH2 };
